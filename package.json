{
  "name": "neuroverlay-cf",
  "version": "1.0.0",
  "description": "NeuroOverlay backend on Cloudflare Workers: Telegram login, per-user overlay settings in D1, and live sync for OBS / TikTok LIVE Studio browser sources via Durable Objects.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "node scripts/migrate.mjs --local",
    "db:migrate:remote": "node scripts/migrate.mjs --remote",
    "make-admin": "node scripts/make-admin.mjs",
    "smoke-test": "node scripts/smoke-test.mjs",
    "unit-test": "node --no-warnings scripts/unit-test.mjs",
    "test": "npm run unit-test && npm run xss-test && npm run smoke-test",
    "xss-test": "node scripts/xss-test.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jsdom": "^30.1.0",
    "wrangler": "^4.130.0",
    "ws": "^8.21.3"
  }
}
